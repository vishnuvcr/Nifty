name: SENSEX S5 Walk-Forward Dispatcher

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      slippage:
        description: "Adverse slippage points per executed option leg"
        required: true
        default: "0.5"

permissions:
  contents: write

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout SENSEX S5
        uses: actions/checkout@v4
        with:
          ref: research/sensex-s5-wfa-backtest-v1
          fetch-depth: 1
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Record run start
        run: |
          mkdir -p reports/sensex_options
          date -u +"%Y-%m-%dT%H:%M:%SZ" > reports/sensex_options/RUN_STARTED_UTC.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/sensex_options/RUN_STARTED_UTC.txt
          git commit -m "ci: mark SENSEX S5 backtest started [skip ci]"
          git push origin HEAD:research/sensex-s5-wfa-backtest-v1
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip pandas numpy pyarrow
          python -m pip install -e .
      - name: Cache SENSEX dataset
        uses: actions/cache@v4
        with:
          path: data/raw_sensex
          key: sensex-options-${{ runner.os }}-2026-09-v3

      - name: Fetch independent daily SENSEX history
        run: |
          mkdir -p data/external
          python - <<'PY'
          import json, urllib.request
          from datetime import datetime, timezone
          import pandas as pd
          start = int(datetime(2021, 1, 1, tzinfo=timezone.utc).timestamp())
          end = int(datetime(2026, 9, 21, tzinfo=timezone.utc).timestamp())
          url = f"https://query1.finance.yahoo.com/v8/finance/chart/%5EBSESN?period1={start}&period2={end}&interval=1d&events=history&includeAdjustedClose=true"
          with urllib.request.urlopen(url, timeout=30) as r:
              payload = json.load(r)
          result = payload["chart"]["result"][0]
          ts = pd.to_datetime(result["timestamp"], unit="s", utc=True).tz_convert("Asia/Kolkata")
          q = result["indicators"]["quote"][0]
          df = pd.DataFrame({"timestamp": ts, "open": q["open"], "high": q["high"], "low": q["low"], "close": q["close"]})
          df = df.dropna(subset=["close"]).copy()
          df["trading_day"] = df["timestamp"].dt.normalize().dt.tz_localize(None)
          df.to_parquet("data/external/sensex_daily_yahoo.parquet", index=False)
          print("daily rows:", len(df), "from", df["trading_day"].min(), "to", df["trading_day"].max())
          PY

      - name: Fetch SENSEX 1-minute dataset
        run: |
          if [ -f data/raw_sensex/index/SENSEX.parquet ]; then
            echo "Using cached SENSEX dataset."
            exit 0
          fi
          python -m pip install huggingface_hub
          mkdir -p data/raw_sensex
          hf download thetrademarkk/india-index-options-1m index/SENSEX.parquet --repo-type dataset --local-dir data/raw_sensex
          for year in 2024 2025; do
            hf download thetrademarkk/india-index-options-1m --include "options/SENSEX/$year-*.parquet" --repo-type dataset --local-dir data/raw_sensex
          done
          for month in 01 02 03 04 05 06 07; do
            hf download thetrademarkk/india-index-options-1m --include "options/SENSEX/2026-$month-*.parquet" --repo-type dataset --local-dir data/raw_sensex
          done
          test -f data/raw_sensex/index/SENSEX.parquet
          test -d data/raw_sensex/options/SENSEX
      - name: Backtest development
        run: python scripts/sensex_backtest_v1.py --options-dir data/raw_sensex/options/SENSEX --index-path data/raw_sensex/index/SENSEX.parquet --mc-index-path data/external/sensex_daily_yahoo.parquet --split development --slippage "${{ inputs.slippage || '0.5' }}" --out-dir reports/sensex_options
      - name: Backtest validation
        run: python scripts/sensex_backtest_v1.py --options-dir data/raw_sensex/options/SENSEX --index-path data/raw_sensex/index/SENSEX.parquet --mc-index-path data/external/sensex_daily_yahoo.parquet --split validation --slippage "${{ inputs.slippage || '0.5' }}" --out-dir reports/sensex_options
      - name: Backtest holdout
        run: python scripts/sensex_backtest_v1.py --options-dir data/raw_sensex/options/SENSEX --index-path data/raw_sensex/index/SENSEX.parquet --mc-index-path data/external/sensex_daily_yahoo.parquet --split holdout --slippage "${{ inputs.slippage || '0.5' }}" --out-dir reports/sensex_options
      - name: Publish reports
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/sensex_options
          git commit -m "research: publish SENSEX S5 backtest results [skip ci]" || true
          git push origin HEAD:research/sensex-s5-wfa-backtest-v1