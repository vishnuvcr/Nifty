name: adaptive-regime-strategy-router

on:
  workflow_run:
    workflows: ["strategy-regime-lab-v2"]
    types: [completed]
  push:
    branches: ["research/monte-carlo-wfa-v1"]
    paths:
      - scripts/run_regime_strategy_router_v3.py
      - .github/workflows/adaptive-regime-strategy-router.yml
  workflow_dispatch:

jobs:
  router:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e .
      - name: Resolve V2 run
        id: v2
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            run_id="${{ github.event.workflow_run.id }}"
          else
            run_id="$(gh run list --repo "$GITHUB_REPOSITORY" --workflow "strategy-regime-lab-v2" --branch research/monte-carlo-wfa-v1 --status success --limit 1 --json databaseId --jq '.[0].databaseId')"
          fi
          test -n "$run_id"
          echo "run_id=$run_id" >> "$GITHUB_OUTPUT"
      - name: Download V2 artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p input
          gh run download "${{ steps.v2.outputs.run_id }}" --repo "${{ github.repository }}" --name strategy-regime-lab-v2-results --dir input
          test -f input/strategy_trades.csv
      - name: Run adaptive regime router
        run: |
          mkdir -p results
          python scripts/run_regime_strategy_router_v3.py \
            --trades input/strategy_trades.csv \
            --out-dir results \
            --cost-per-contract 2 \
            --dev-end-year 2022 \
            --validation-end-year 2024 \
            --final-start-year 2025 \
            --dev-min-n 30 \
            --validation-min-n 10
      - name: Publish headline
        run: |
          cat results/adaptive_regime_strategy_report.md
          echo
          cat results/regime_strategy_results.csv
      - uses: actions/upload-artifact@v4
        with:
          name: adaptive-regime-strategy-router
          path: results/
          retention-days: 30
